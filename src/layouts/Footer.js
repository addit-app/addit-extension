import React from 'react'
import {
  Layout,
} from 'antd'

const { Footer } = Layout

const FooterWrapper = () => {
  return (
    <Footer
      style={{
        textAlign: 'center',
      }}
    >
      &copy; ADDIT
    </Footer>
  )
}

export default FooterWrapper
